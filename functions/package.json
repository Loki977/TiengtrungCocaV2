{
  "name": "tiengtrungcoca-admin-functions",
  "version": "1.0.0",
  "private": true,
  "engines": { "node": "20" },
  "main": "index.js",
  "scripts": {
    "check": "node --check index.js && node --check admin-core.js",
    "test": "npm run check && node test/admin-core.test.js",
    "deploy": "firebase deploy --only functions",
    "serve": "firebase emulators:start --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^5.1.1"
  }
}
